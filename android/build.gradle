buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.1.2"  // Make sure this version matches the one required by your project
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.22"  // Ensure the Kotlin version matches your requirements
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
