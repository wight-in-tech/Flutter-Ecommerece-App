class SelectionPopupModel {
  SelectionPopupModel(
      {this.id, required this.title, this.value, this.isselected = false}) {
    // TODO: implement SelectionPopupModel
    throw UnimplementedError();
  }
  int? id;
  String title;
  dynamic value;
  bool isselected;
}
